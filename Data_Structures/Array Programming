Rotate the given array by k position.
a[7] = {1,2,3,4,5,6,7,8}, n = 8, k =2;

for(i = 0; i < gcd(n, k); i++)
{
 temp = a[i];
  j = i;
  while(1)
  {
   d = ((j+k) % n);
   
   if(d == i)
    break;
    
    a[j] = a[d];
    j = d;
  }
  a[j] = temp;
}

int gcd(int a, int b)
{
 while(a!=b)
 {
   if(a > b)
    a -= b;
   else
    b -= a;
 }
 return a;
}
